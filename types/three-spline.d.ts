// three-spline = npm-Alias auf three@0.149 (nur fuer @splinetool/loader und
// die Talos-Buehne). Bewusst untypisiert: die 0.185-Typen der Haupt-App passen
// nicht auf die 0.149-API (outputEncoding, sRGBEncoding usw.).
declare module "three-spline";
